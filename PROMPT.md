Help me copy local Claude Desktop Code chats between my own profiles using https://github.com/yugjindal22/claude-chat-copy.

1. Clone this repository into a new local directory. Read README.md, DETAILS.md, USE.md and the code before running it. Use local terminal access, not cloud connectors. Do not run a downloaded installer or upload any chats.
2. Run `python3 claude_chat_copy.py --list` to discover local account directories. Only show the relevant profile names, project folders and chat titles. Never print message contents, credentials or cookies. Ask which source, destination and project I want if I haven't specified them; do not infer account ownership from an opaque directory ID.
3. If a destination account directory is missing, ask me to sign into that profile and create one harmless local Code chat first. Stop before copying until that is done. Never copy the primary's browser storage or login into another profile.
4. Use explicit `--source`, `--target` and `--project` values from discovery. Add repeated `--session` arguments if I requested specific chats. Run without `--apply` first and show the selected titles and shared working folder. If I use a custom Claude Code config directory, use `--config-dir` and verify it holds the transcripts; do not silently choose another account's history.
5. Ensure I have a local backup of the selected history before applying. Keep it outside the repository and restrict access. Do not copy .credentials.json, cookies, Keychain entries or account configuration into the backup. Treat history as sensitive because it can contain secrets I previously pasted.
6. Check running apps. Never quit or kill Claude Desktop or its helpers. Ask me to finish tasks and quit Desktop myself. If you are inside Claude Desktop, give me the exact previewed command with `--apply` to run in Terminal after quitting. A CLI/VS Code assistant can continue only if it is not writing the source transcripts. Do not bypass the tool's running-app guard.
7. Once my requested selection is clear and Desktop is closed, run the previewed command with `--apply`. Verify the receipt contains the expected count and the originals are unchanged. Keep the receipt private. Reopen the destination launcher only, and have me check the copied chats. Do not send prompts on my behalf to test them.

Only copy local Code conversations. Do not promise claude.ai cloud-chat migration, full environment isolation, or account-policy approval. The copies share the original project files; warn me before simultaneous edits to that folder. Do not use this to evade usage limits or move employer/client data without permission.
